# REPO-1 scrub gate — report (2026-07-30)
*Per REPO_SPEC §5 / REPO_GO REPO-1. Run over the private repo before the public flip. Pass/fail per item; no silent fixes. Moves toward `record/` in REPO-2.*

| # | Check | Result |
|---|-------|--------|
| 1 | Secret scan (gitleaks not installed; manual patterns `sk-`/`gho_`/`AKIA`/`BEGIN…PRIVATE KEY`/`api_key=` over tracked text at HEAD) | **PASS** — no secrets. Full-history automated scan (gitleaks) still recommended before flip. |
| 2 | Secret-bearing files tracked (`.env`, `*.key`, `*.pem`, `id_rsa`) | **PASS** — none. |
| 3 | Account-export zip in history (`data-93119b11…`, `*.zip`) | **PASS** — never entered git. Quarantined to `D:\Niels\Proj-2026\archive\` (out of tree); Dropbox second copy confirmed. |
| 4 | Absolute local paths in tracked text | **FINDING** — `ASSET_SPEC_GUIDE.md` and `FIGURES_NOTE.md` expose `D:\Niels\…` paths; `ASSET_SPEC_GUIDE.md:17` names a local `.env` location (not the key). See actions. |
| 5 | Stray emails in manuscript | **PASS** — only the committer `nielsbj@gmail.com` in git metadata (RULING: author email public or masked — REPO_SPEC §7.4). |
| 6 | Beta-reader material | **PENDING** — no obvious beta content in tracked files; a targeted name/email sweep waits on the beta list (beta material must never enter the repo, ever). |
| 7 | History size / rebuilt-PDF weight | **PENDING** — `billiard_ball_universe.pdf` is committed per release; measure `.git` growth before flip; prefer keeping history over rewriting (§5.4). |

## Actions taken this pass
- **`ASSET_SPEC_GUIDE.md` removed at HEAD** (Niels's ruling): it is shared cross-project infrastructure (the image-generation gateway), documents other projects, and names a local `.env` path — the book repo needs only the provenance of its own images, which now lives self-contained in `art/cover/PROVENANCE.md` + `cover_tiles.spec.json`. Its presence in earlier history is **declared clutter — no rewrite**.

## Open for the "decide while private" list
- `FIGURES_NOTE.md` local path (proofviz studio) — genericize or accept.
- Committer email public vs masked (§7.4).
- Full-history gitleaks pass + size measurement before the flip.
